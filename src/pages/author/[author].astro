---
export function getStaticPaths() {
  return [
    {params: {author: 'clifford'}},
    {params: {author: 'rover'}},
    {params: {author: 'spot'}},
  ];
}

const { author } = Astro.params;
---
<div>Good dog, {author}!</div>
